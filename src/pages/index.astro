---
import HomeView from '@/components/pages/views/HomeView.astro';
import { defaultLocale } from '@/i18n';

// The landing page for the default locale. Additional locales are served by
// `src/pages/[locale]/index.astro`, which renders the same view.
---

<HomeView locale={defaultLocale} />
