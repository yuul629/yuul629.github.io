---
import AboutView from '@/components/pages/views/AboutView.astro';
import { defaultLocale } from '@/i18n';

// The About page for the default locale. Additional locales are served by
// `src/pages/[locale]/about.astro`, which renders the same view.
---

<AboutView locale={defaultLocale} />
