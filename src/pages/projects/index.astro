---
import ProjectsIndexView from '@/components/projects/views/ProjectsIndexView.astro';
import { defaultLocale } from '@/i18n';

// The projects landing page for the default locale. Additional locales are
// served by `src/pages/[locale]/projects/index.astro`, which renders the same view.
---

<ProjectsIndexView locale={defaultLocale} />
