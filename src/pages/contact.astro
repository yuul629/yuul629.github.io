---
import ContactView from '@/components/pages/views/ContactView.astro';
import { defaultLocale } from '@/i18n';

// The Contact page for the default locale. Additional locales are served by
// `src/pages/[locale]/contact.astro`, which renders the same view.
---

<ContactView locale={defaultLocale} />
