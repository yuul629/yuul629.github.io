---
import BlogIndexView from '@/components/blog/views/BlogIndexView.astro';
import { defaultLocale } from '@/i18n';

// The blog landing page for the default locale. Additional locales are served
// by `src/pages/[locale]/blog/index.astro`, which renders the same view.
---

<BlogIndexView locale={defaultLocale} />
