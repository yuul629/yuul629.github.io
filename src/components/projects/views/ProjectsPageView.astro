---
/**
 * ProjectsPageView — a paginated projects index page (page 2..N).
 *
 * Shared body for the default-locale route (`/projects/page/N`) and the
 * locale-prefixed route (`/<locale>/projects/page/N`). The page number and
 * total are resolved in each route's `getStaticPaths` and passed in; URLs are
 * built locale-aware via `lib/projects`.
 */
import PageLayout from '@/layouts/PageLayout.astro';
import Icon from '@/components/ui/primitives/Icon/Icon.astro';
import Badge from '@/components/ui/data-display/Badge/Badge.astro';
import Button from '@/components/ui/form/Button/Button.astro';
import { Hero } from '@/components/hero';
import LetterGlitchBand from '@/components/patterns/LetterGlitchBand.astro';
import ProjectCard from '@/components/projects/ProjectCard.astro';
import ProjectTagList from '@/components/projects/ProjectTagList.astro';
import Pagination from '@/components/blog/Pagination.astro';
import { t, defaultLocale, localizedPath } from '@/i18n';
import {
  collectTopProjectTags,
  getProjectsBaseUrl,
  getProjectsPageUrl,
  getVisibleProjects,
  PROJECT_TAG_CLOUD_LIMIT,
  PROJECTS_PER_PAGE,
} from '@/lib/projects';

interface Props {
  /** Locale whose projects this page renders. Defaults to the site default. */
  locale?: string;
  /** 1-based page number (always >= 2 for this view). */
  page: number;
  /** Total number of index pages for this locale. */
  totalPages: number;
}

const { locale = defaultLocale, page, totalPages } = Astro.props;

const projects = await getVisibleProjects(locale);
const start = (page - 1) * PROJECTS_PER_PAGE;
const pageProjects = projects.slice(start, start + PROJECTS_PER_PAGE);

const topTags = collectTopProjectTags(projects, PROJECT_TAG_CLOUD_LIMIT);

const projectsHref = getProjectsBaseUrl(locale);
const contactUrl = localizedPath('/contact', locale);
const aboutUrl = localizedPath('/about', locale);
---

<PageLayout
  title={t('projects.pageMetaTitle', locale, { page })}
  description={t('projects.pageMetaDescription', locale, { page, total: totalPages })}
  eagerReveal
  footerBackground="secondary"
>
  <Hero layout="centered" size="sm" class="isolate" showHeroHalo>
    <Badge slot="badge" variant="brand" pill>
      <Icon name="layers" size="sm" />
      {t('projects.pageBadge', locale, { page, total: totalPages })}
    </Badge>

    <h1 slot="title">{t('projects.title', locale)}</h1>

    <p slot="description">
      {t('projects.heroDescription', locale)}
    </p>
  </Hero>

  <section class="bg-background-secondary py-[var(--space-section-md)] border-t border-border">
    <div class="mx-auto max-w-6xl px-6 flex flex-col gap-8">
      <div class="flex flex-col items-center gap-4" data-reveal>
        <a
          href={projectsHref}
          class="group inline-flex items-center gap-2 rounded-lg border border-brand-500/40 bg-transparent px-4 py-2 text-sm font-medium text-brand-700 dark:text-brand-400 hover:border-brand-500/80 hover:bg-brand-500/10 transition-colors"
        >
          <Icon name="arrow-left" size="sm" />
          {t('projects.allProjects', locale)}
        </a>

        {topTags.length > 0 && (
          <ProjectTagList tags={topTags} locale={locale} class="justify-center" />
        )}
      </div>

      <div class="grid gap-6 md:grid-cols-2">
        {pageProjects.map((project, i) => (
          <ProjectCard project={project} index={i} locale={locale} />
        ))}
      </div>

      <Pagination
        currentPage={page}
        totalPages={totalPages}
        href={(p) => getProjectsPageUrl(p, locale)}
      />
    </div>
  </section>

  <!-- CTA -->
  <section class="relative z-10 py-[var(--space-section-md)] bg-background">
    <!-- Mobile: flat section, no card -->
    <div class="glitch:hidden mx-auto max-w-2xl px-6 text-center" data-reveal>
      <Badge variant="brand" pill class="mb-6">
        <Icon name="message-circle" size="sm" />
        {t('projects.ctaBadge', locale)}
      </Badge>
      <h2 class="font-display text-4xl font-bold text-foreground mb-4 text-balance">
        {t('projects.ctaHeading', locale)}
      </h2>
      <p class="text-lg text-foreground-muted mb-8 text-balance">
        {t('projects.ctaLead', locale)}
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button size="lg" href={contactUrl} class="cta-btn-brand">
          {t('projects.ctaPrimary', locale)}
          <Icon name="arrow-right" size="sm" />
        </Button>
        <Button size="lg" variant="brand-outline" href={aboutUrl}>
          {t('projects.ctaSecondary', locale)}
          <Icon name="arrow-right" size="sm" />
        </Button>
      </div>
    </div>

    <!-- Desktop: contained LetterGlitch band with glass card -->
    <LetterGlitchBand>
      <h2 class="font-display text-4xl font-bold text-foreground mb-4 text-balance">
        {t('projects.ctaHeading', locale)}
      </h2>
      <p class="text-lg text-foreground-muted mb-8 text-balance">
        {t('projects.ctaLead', locale)}
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button size="lg" href={contactUrl} class="hero-btn-brand">
          {t('projects.ctaPrimary', locale)}
          <Icon name="arrow-right" size="sm" />
        </Button>
        <Button size="lg" variant="outline" href={aboutUrl}>
          {t('projects.ctaSecondary', locale)}
          <Icon name="arrow-right" size="sm" />
        </Button>
      </div>
    </LetterGlitchBand>
  </section>
</PageLayout>
